import React, { Component } from 'react'
import { Header, Image, Table, Divider } from 'semantic-ui-react'
export default class CartElement extends Component {
    render() {
        
        return (
            <div>
                {/* <img style={{width: "100px",height:"100px" }} src={this.props.clothing.picture}/>
                <h2>{this.props.clothing.name}</h2>
                <h2>{this.props.clothing.price}</h2> */}
            </div>
            
        )
    
    }
}
